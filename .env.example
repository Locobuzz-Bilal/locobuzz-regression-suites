# Email Configuration for Suite Reports
EMAIL_USER=your-email@example.com
EMAIL_PASS=your-app-password
EMAIL_TO=recipient1@example.com,recipient2@example.com

# ChromeDriver Path (Windows only - optional, webdriver-manager used if not set)
CHROMEDRIVER_PATH=C:/path/to/your/chromedriver.exe

# Test Credentials (if needed)
# Add any other environment variables your tests need
